Pulse Weaver Streaming Studio - Private Beta 1.12.7

Camera: Portrait now shares the native landscape editor, docks, source menus
and transform controls, while editing a separate 1080 x 1920 canvas.
Selecting a portrait scene changes the editor preview, not programme output.
Audio mixer and transition presets remain shared.

Close all Pulse Weaver windows and run the setup EXE over the existing install.
Do not uninstall first. Setup preserves the existing config directory and
creates a unique pre-upgrade ZIP under the installation's config-backups folder.
The upgrade stops if the backup cannot be created. Backups may include private
account settings: keep them local and do not share them.

Existing scenes and source transforms are reused, not rebuilt or auto-fitted.
Synthetic legacy-format retention and installer preservation tests pass.
End-to-end upgrade testing with your actual scene collection remains pending.
Check both canvases before going live. This is an unreleased local beta build.
