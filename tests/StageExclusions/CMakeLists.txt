cmake_minimum_required(VERSION 3.28)
project(PulseStageExclusionTests LANGUAGES CXX)
set(CMAKE_CXX_STANDARD 17)
get_filename_component(REPO_ROOT "../.." ABSOLUTE BASE_DIR "${CMAKE_CURRENT_SOURCE_DIR}")
set(OBS_ROOT "${REPO_ROOT}/engine/obs-studio")
set(OBS_BUILD "${OBS_ROOT}/build_pw_vs1714_sdk22621")
find_package(Qt6 REQUIRED COMPONENTS Core)
add_executable(PulseStageExclusionTests main.cpp)
target_include_directories(PulseStageExclusionTests PRIVATE "${OBS_ROOT}/libobs" "${OBS_BUILD}/config")
target_link_libraries(PulseStageExclusionTests PRIVATE Qt6::Core "${OBS_BUILD}/libobs/RelWithDebInfo/obs.lib")
